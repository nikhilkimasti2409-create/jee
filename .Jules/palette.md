
## 2026-08-21 - Accessible Custom Toggles
**Learning:** Custom toggle switches built with `div` elements lack necessary keyboard navigation and screen reader support.
**Action:** Always use semantic `<button type="button" role="switch">`, include `aria-checked`, and provide clear `focus-visible` styles for custom toggle components.
